################################################################
# Dash. hardware monitoring 
################################################################
services:
  dash:
    image: mauricenino/dashdot:latest
    container_name: dash.
    restart: unless-stopped
    privileged: true
    ports:
      - '4691:3001'
    volumes:
      - /:/mnt/host:ro
